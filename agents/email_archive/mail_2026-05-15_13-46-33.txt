Subject: Re: bizzy refactor strat

Нашёл. Вот что говорит календарь:

**Вт 19.05 | 18:30 | №6 Да/Нет | №44 | POST без тела — 400 или 500? А если с пустым JSON?**

Проблема: **контент поста №44 нигде не записан.** В файлах форматов (PART5, CONTENT) есть 4 поста для формата «Да/Нет» — про GET с телом, PUT, DELETE, rate limiting. Про POST без тела — **ноль**. Календарь помечен как «ГОТОВ К ПУБЛИКАЦИИ», но текста нет.

Это я просчитался — запланировал, но не написал.

---

Держи пост. Формат №6, мясной, ситуативный, 300 знаков:

**POST без тела. Что вернёт сервер?**

Отправляю `POST /api/users` без тела. Просто хедеры.

Сервер:
```
HTTP/1.1 500 Internal Server Error
```

А если пустой JSON?

```
POST /api/users
Content-Type: application/json

{}
```

Тоже 500.

Вопрос: **Чей это баг?**

А) Бэкенда — должен отдавать 400 Bad Request
Б) Клиента — кто шлёт пустоту, тот идиот
В) Не баг — это норм реакция на пустой запрос

👇 Пиши свой вариант.

Через час разберу.

---

Если хочешь — могу отправить как файл в Obsidian, чтобы был в базе. Или просто копируй и публикуй в 18:30 завтра.